# PRODUCTION deps — installed by Vercel's Python builder for the serverless
# function. Deliberately slim: NO scikit-learn/scipy/numpy (148 MB, over Vercel's
# 250 MB limit). The two local models run on pure-python + python-crfsuite; the
# heavy models (DistilBERT, Claude) are remote API calls. Dev/test/training deps
# live in api/requirements.txt.
fastapi==0.115.*
pydantic==2.*
vaderSentiment==3.3.2
python-crfsuite==0.9.*
httpx==0.27.*
anthropic==0.40.*
