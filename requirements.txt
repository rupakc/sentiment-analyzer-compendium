# Vercel's Python builder reads this root-level requirements.txt and its
# parser doesn't support `-r` includes — keep in sync with api/requirements.txt.
fastapi==0.115.*
vaderSentiment==3.3.2
scikit-learn==1.5.*
sklearn-crfsuite==0.5.0
httpx==0.27.*
anthropic==0.40.*
pydantic==2.*
uvicorn==0.30.*
pytest==8.*
respx==0.21.*
