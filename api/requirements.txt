# DEV deps — local run + CI tests + model training. The production subset that
# Vercel ships is the slimmer root requirements.txt (no scikit-learn/scipy).
fastapi==0.115.*
pydantic==2.*
vaderSentiment==3.3.2
python-crfsuite==0.9.*
httpx==0.27.*
anthropic==0.40.*
uvicorn==0.30.*
scikit-learn==1.5.*  # training only — scripts/train_models.py exports slim artifacts
pytest==8.*
respx==0.21.*
